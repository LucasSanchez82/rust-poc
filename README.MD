⚠️ Do not forget to install the Cranelift codegen backend using `rustup component add rustc-codegen-cranelift-preview --toolchain nightly`.

make nihtly locally
`rustup component add rustc-codegen-cranelift-preview --toolchain nightly`

## Prerequisites
Lib to install ( use `aptitude` to see packages tha provide this lib)
- libssl.so.3
- libcrypto.so.3
- libgcc_s.so.1
- libm.so.6
- libc.so.6