meta {
  name: Get status of container
  type: http
  seq: 2
}

get {
  url: {{base_url}}/docker/mariadb?container_name=container_name
  body: none
  auth: inherit
}

params:query {
  container_name: container_name
}

settings {
  encodeUrl: true
  timeout: 0
}
