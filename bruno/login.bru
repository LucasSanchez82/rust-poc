meta {
  name: login
  type: http
  seq: 4
}

post {
  url: {{base_url}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "tes2t@gmail.Com",
    "password": "passwordForToto"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
