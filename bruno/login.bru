meta {
  name: login
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "tes2t@gmail.Com",
    "password": "passwordForToto"
  }
}

script:post-response {
  let data = res.body;
  bru.setVar("auth_token", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
